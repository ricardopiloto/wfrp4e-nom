# Daily log — 2026-05-05

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation).

## OpenSpec apply

### `fix-inquisidor-school-skills-display`

- **Runtime**: Updated `scripts/inquisidor-school-handler.js` — school skills append to **`system.skills`** (replacing **`system.addedSkills`**) because the WFRP4e career item template binds the editable Skills list to `system.skills` only (`Data/systems/wfrp4e/templates/sheets/item/types/career.hbs`).
- **Spec**: Merged deltas into `openspec/specs/estalian-inquisidor-school-selection/spec.md` (new visibility requirement + updated “career-only lists” wording).
- **Release note**: `CHANGELOG.md` Unreleased entry for school skills visibility.
- **Validation**: `openspec validate fix-inquisidor-school-skills-display --strict`; `openspec validate estalian-inquisidor-school-selection --type spec --strict`; in-game UX cross-check deferred to maintainer if desired.

### `grant-inquisidor-school-skills-on-actor`

- **Runtime**: `scripts/inquisidor-school-handler.js` — after career list updates succeed, iterates **`pack.skills`** only (**two** names per school) and calls `game.wfrp4e.utility.findSkill` + `actor.createEmbeddedDocuments("Item", [skill], { career: true })` when no matching owned `skill` exists; skips duplicate names case-insensitive; skips `setFlag` if embed fails (_onAddUntrainedSkill parity in `systems/wfrp4e/wfrp4e.js`).
- **Spec**: Updated `openspec/specs/estalian-inquisidor-school-selection/spec.md` (`Purpose`, **Perícias treinadas**, **Perícias visíveis**, **Efeito**, **Registo**).
- **Release note**: `CHANGELOG.md` Unreleased line for embedded school skills only.
- **Validation**: `openspec validate grant-inquisidor-school-skills-on-actor --strict`; `openspec validate estalian-inquisidor-school-selection --type spec --strict`; in-game pass optional.

### `remove-inquisidor-actor-school-skill-embed`

- **Runtime**: Removed `embedActorSchoolSkillIfMissing` and post–career `createEmbeddedDocuments` path from `scripts/inquisidor-school-handler.js`; `inquisidorSchool` sets after career `Item#update` success only (matches pre-grant semantics).
- **Spec**: `openspec/specs/estalian-inquisidor-school-selection/spec.md` — dropped **Perícias da escola treinadas…**; reverted **Purpose**, **Efeito**, **Perícias visíveis**, **Registo** to career-only / no actor **`skill`** mandate for the school flow.
- **Release note**: `CHANGELOG.md` Unreleased — *Removed* actor skill embed line; kept `fix-inquisidor-school-skills-display` note.
- **Validation**: `openspec validate remove-inquisidor-actor-school-skill-embed --strict`; `openspec validate estalian-inquisidor-school-selection --type spec --strict`.

### `release-v1.1.6-readme`

- **Documentation**: Rewrote **`README.md`** in English — goals, content list, automation table (pickers + Inquisidor career lists), deferred/manual section (Path of Flame unloaded, effects folder paste-only); requirements match **`module.json`** (`wfrp4e-core`, `wfrp4e-more-subspecies`).
- **Release**: **`module.json`** version **1.1.6**; **`download`** URL **v1.1.6**; **`CHANGELOG.md`** **[1.1.6]** aggregates prior Unreleased (Inquisidor career `system.skills` fix, removal of actor skill embed) plus README packaging note; **Unreleased** cleared.
