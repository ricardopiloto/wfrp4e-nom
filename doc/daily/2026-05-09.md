# Daily log — 2026-05-09

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation). Logged retroactively for **v1.3.0** so May work is not lost between **1.2.0** and the August career-content push.

## OpenSpec apply

### `register-nom-human-subspecies`

- **Runtime**: Added **`scripts/nom-subspecies-registry.js`** — `Hooks.once("init")` merges NoM Human nationalities into **`game.wfrp4e.config.subspecies.human`**; **`module.json`** drops hard dependency on **`wfrp4e-more-subspecies`**.
- **Tables**: Marienburger / Wastelander nationality career table + migrate prefix **Westerland**.
- **Validation**: OpenSpec archived change; chargen column ↔ **`flags.wfrp4e.column`** parity with Core.

### `remap-nom-career-rows-core-catalog`

- **Tooling**: Added **`scripts/remap-nom-career-rows-to-core.mjs`** + npm **`career-tables:remap-core-names`**; remapped supplement-style nationality row names to Core career titles before migrate.
- **Tables**: Updated **`Career___Human__*.json`** so Tier-1 Core careers resolve (e.g. Artillerist → Engineer where appropriate).
- **Validation**: **`reports/career-rolltable-unmatched.txt`** clean for remapped labels after migrate.

### `add-chosen-hound-mark-talent-items`

- **nom-items**: Ten mutation talents for **Chosen of the Hound** in folder **NoM - Talents**.
- **Validation**: Items present under **`packs-src/nom-items/`** with shared table source **`tI1syOkdXB0Y5UOV`**.

### `add-chosen-hound-mark-talent-effects`

- **nom-items**: ActiveEffects on mark talents (AP, Iron Skin Agility, Multiple Legs move, trait **`addItems`**, …).
- **Validation**: Effects authored per change design; Multiple Arms / Spiked charge rider left descriptive where noted.

### `wire-chosen-hound-rolltable-talent-links`

- **Tables**: **`Chosen_of_the_Hound_tI1syOkdXB0Y5UOV.json`** rows are **`type: document`** → **`nom-items`**.

### `shorten-chosen-hound-table-result-names`

- **Tables**: **`results[].name`** shortened to talent title matching **`Item.name`**.

### `restore-chosen-hound-rolltable-result-text`

- **Tables**: Full mutation line kept in **`results[].text`** for chat/previews; rules remain on the Item.

### `chore-release-workflow-placeholders`

- **CI / module.json**: Release workflow substitutes **`${version}`** / manifest / download placeholders (aligned with homebrew-qol); **`npm run packs:build`** before zip.
