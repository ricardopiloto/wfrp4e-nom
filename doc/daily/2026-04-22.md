# Daily log — 2026-04-22

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation).

## OpenSpec apply

### `update-knightly-virtue-virtue-only-sheet-name`

- **Knightly Virtue sheet naming**: `scripts/knightly-virtue.js` — after confirmation, embedded talent **`name`** is **`selectedVirtueName`** only (no `Knightly Virtue (…)` prefix) in both fallback-create and compendium/world replacement paths.
- **Virtue detection**: `hasStoicismVirtue` / `hasPenitentVirtue` accept exact **`Virtue of Stoicism`** / **`Virtue of the Penitent`**, legacy `Knightly Virtue (Virtue of …)` names, and existing **`wfrp4e-nom`** effect flags; create-hook comments clarify generic base names via **`isBaseKnightlyVirtueName`** only.
- **Docs**: `README.md` (Knightly Virtue section); `CHANGELOG.md` (**Unreleased** bullet).
- **Spec**: `openspec/specs/knightly-virtue-selection/spec.md` updated to match behaviour; `openspec/changes/update-knightly-virtue-virtue-only-sheet-name/tasks.md` checklist — implementation and doc tasks done; manual Foundry QA (**2.3**) and **`openspec archive`** at release (**3.1**) still open.
- **Validation**: `openspec validate update-knightly-virtue-virtue-only-sheet-name --strict` (OK); linter clean on `knightly-virtue.js`; Foundry UI session not run here (**2.3** deferred).

### `add-grail-virtue-selection-flow`

- **Handler**: new `scripts/grail-virtue.js` — detects exact **`Grail Virtue`** on `talent`/`skill`/`trait`; **`NomTalentRadioPicker`** with **14** fixed descriptors + manual input (Knightly-style); **`replaceGrailVirtueOnActor`** mirrors **Mark of the Gods** (world/compendium lookup, exact sheet **`name`**, merge **ActiveEffect** from replacement + generic on hit path).
- **Hooks**: `createItem` and `createEmbeddedDocuments` with **300ms** / retry **200ms**, **`grailVirtueProcessing`** `Set`, **`actor.isOwner`** only.
- **Manifest**: `module.json` → **`esmodules`** inserts **`scripts/grail-virtue.js`** immediately after **`scripts/talent-option-picker-app.js`**.
- **Docs**: `README.md` (Features, Knightly/Martial/Mark cross-references, new **Grail Virtue** section with numbered list); `CHANGELOG.md` (**Unreleased**).
- **Canonical specs**: new `openspec/specs/grail-virtue-selection/spec.md`; **`openspec/specs/nom-shared-talent-picker/spec.md`** updated to **four** consuming flows.
- **Change tasks**: `openspec/changes/add-grail-virtue-selection-flow/tasks.md` — sections **1** and **2.1–2.3** complete; **2.4** (Foundry manual QA) and **3.1** (`openspec archive` after merge) left open.
- **Validation**: `openspec validate add-grail-virtue-selection-flow --strict` (OK); linter clean on `grail-virtue.js`; Foundry UI not run (**2.4** deferred).

### `chore-release-1.0.7`

- **Version**: **`module.json`** → **1.0.7**; **`download`** → **`v1.0.7`** GitHub release asset URL.
- **Changelog**: moved **2026-04-22** work from **Unreleased** to **`[Version 1.0.7]`** (Grail Virtue, Knightly Virtue naming, README/OpenSpec notes); **Unreleased** cleared for backlog.
- **Basis**: release notes aligned with **`doc/daily/2026-04-22.md`** OpenSpec apply entries above.
- **Validation**: N/A (metadata and changelog only); publish **`v1.0.7`** on GitHub before players rely on the new **`download`** URL.

### `add-martial-artist-flame-path-tier-automation`

- **Tier & WFRP4e field**: **`scripts/martial-artist-path-flame.js`** — purchase count from **`system.Advances`** / **`item.Advances`** (confirmed against bundled **`systems/wfrp4e/wfrp4e.js`**); **`getFlamePathTier`** returns **0–4**; item match at ship time was compound **`Martial Artist (Path of the Flame)`** (later broadened to **`Path of the Flame`** + legacy under **`update-martial-artist-sheet-title-path-only`**) for types **`talent`**, **`skill`**, **`trait`**.
- **Effect sync**: Four idempotent **`ActiveEffect`** rows on the path item with **`flags.wfrp4e-nom`** **`maFlameSync`** / **`maFlameSyncTier`**; **`disabled`** follows tier; dedupe on sync.
- **Hooks**: **`wfrp4e:opposedTestResult`** — Unarmed attacker wins → **`Dialog`** (optional **`addCondition('bleeding')`** with **`flamePathNoviceBleed`**); Master tier **Advantage** messaging (enough vs fewer than three). **`wfrp4e:applyDamage`** — Apprentice reminder for **body** + Unarmed + tier **≥2**.
- **Manifest & docs**: **`module.json`** loads **`martial-artist-path-flame.js`** after **`martial-artist.js`**; **`README.md`** (“Path of the Flame automation”); **`CHANGELOG.md`** (**Unreleased**); canonical spec **`openspec/specs/martial-artist-path-of-flame-automation/spec.md`**.
- **Tasks**: **`openspec/changes/.../tasks.md`** — sections **1–4** marked complete; **5.1** (`openspec archive`) left for post-deploy.
- **Validation**: `openspec validate add-martial-artist-flame-path-tier-automation --strict` (OK).

### `add-career-talent-on-nom-picker-replacement`

- **Helper**: new **`scripts/career-talent-registration.js`** — **`resolveCurrentCareerItem`** (embedded **`career`** with **`system.current.value`**, fallback **`actor.currentCareer`**); **`appendTalentNameToCurrentCareer`** uses **`career.system.talents.add({ name })`** + duplicate check on **`talents.list`**; safe no-op when no career or API missing.
- **Integration**: **`scripts/mark-of-the-gods.js`** and **`scripts/martial-artist.js`** import the helper and call it after successful replacement (**`markName`** / created talent **`name`** for Martial Artist).
- **Manifest**: **`module.json`** — **`career-talent-registration.js`** immediately after **`knightly-virtue.js`** (before consumers).
- **Docs & spec**: **`README.md`** (Martial Artist + Mark paragraphs); **`CHANGELOG.md`** (**Unreleased**); canonical **`openspec/specs/nom-career-talent-registration/spec.md`** with **Purpose** + requirements.
- **Tasks**: **`openspec/changes/.../tasks.md`** — sections **1–4** complete; **5.1–5.3** (Foundry manual QA) left for a live world session.
- **Validation**: `openspec validate add-career-talent-on-nom-picker-replacement --strict` (OK); linter clean on touched scripts.

### `update-martial-artist-sheet-title-path-only`

- **Martial Artist sheet name**: `scripts/martial-artist.js` — after the path picker, embedded talent **`name`** is the path only (e.g. **`Path of the Flame`**); **`isChosenMartialPathSheetName`** / create hooks avoid re-firing the picker for catalog paths and legacy **`Martial Artist (<path>)`**.
- **Path of the Flame**: `scripts/martial-artist-path-flame.js` — **`isFlamePathTalent`** accepts canonical **`Path of the Flame`** and legacy compound name; header comment aligned.
- **Docs & specs**: `README.md`, `CHANGELOG.md` (**Unreleased**); canonical **`openspec/specs/martial-artist-selection/spec.md`** and **`openspec/specs/martial-artist-path-of-flame-automation/spec.md`** updated to path-only + legacy behaviour.
- **Tasks**: `openspec/changes/update-martial-artist-sheet-title-path-only/tasks.md` — sections **1–3** and doc items complete; **4.x** (Foundry manual QA) deferred.
- **Validation**: `openspec validate update-martial-artist-sheet-title-path-only --strict` (OK); linter clean on `martial-artist.js` and `martial-artist-path-flame.js`; Foundry UI not run (**4.x** deferred).
