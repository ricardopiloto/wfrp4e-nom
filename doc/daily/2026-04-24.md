# Daily log — 2026-04-24

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation).

## OpenSpec apply

### `update-release-zip-exclusions`

- **Scope**: Updated `.github/workflows/release.yml` packaging exclusions to keep the release ZIP runtime-only by excluding `doc/`, `docs/` (defensive), and `effects/`.
- **Validation**: `openspec validate update-release-zip-exclusions --strict` (OK); local `unzip -l {id}.zip` sanity-check shows no `doc/`, `docs/`, or `effects/` paths.

### `refactor-talent-specialization-hooks`

- **Scope**: Centralized the five “generic talent -> picker -> sheet replacement” flows (Mark of the Gods, Martial Artist, Grail Virtue, Knightly Virtue, Kenjutsu (Style)) into `scripts/talent-specialization-handler.js` (single `createItem` + single `createEmbeddedDocuments` hook). Updated `module.json` `esmodules` to load the shared handler, and reduced `scripts/knightly-virtue.js` to mechanics-only (Stoicism/Penitent) so selection is handled centrally.
- **Validation**: `openspec validate refactor-talent-specialization-hooks --strict` (OK); `openspec validate --all --strict` (OK).

